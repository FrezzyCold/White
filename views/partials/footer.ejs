  </main>

  <footer class="shell footer">
    <div>
      <strong>Whitecore Client</strong> — приватный клиент с упором на антидетект и удобство.
    </div>
    <div class="muted">© <%= year %> · design by RURI</div>
  </footer>

  <script>
    window.WHITECORE = {
      authed: <%= currentUser ? 'true' : 'false' %>
    };
  </script>
  <script src="/js/app.js"></script>
  <% if (typeof pageScript !== 'undefined' && pageScript) { %>
    <script src="/js/<%= pageScript %>"></script>
  <% } %>
</body>
</html>
